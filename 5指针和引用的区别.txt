1.指针是一个变量的地址，类型为int*
引用是一个已经存在的变量的别名，必须定义的时候初始化，不能更改。

2.指针定义后可以为空，可以更改指向的变量，引用是对变量进行引用，必须要初始化，后续不可更改。
3.指针支持算数运算，用于数组的遍历；引用可以理解为某个变量的别名
4.存在多级指针，不存在多级引用
5.使用的时候指针作为打印对象、返回值需要检查是否为空，引用作为返回值的时候需要注意是否返回的不是局部变量
6.指针的适用更宽松，引用的适用更安全，但是要满足多个条件。