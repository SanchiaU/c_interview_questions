static_cast	相关类型间的编译期转换（基本类型、父子类、有转换规则的类）	需要运行时类型检查时（用 dynamic_cast）
dynamic_cast	多态类型的安全向下转换 / 交叉转换，需要运行时确认	非多态类型、性能敏感的热点代码
const_cast	只在"去掉/添加 const 做接口适配"时使用	想修改本来 const 的对象（UB）
reinterpret_cast	位级别的重新解释、底层/系统编程	上层业务代码 99% 不该出现